[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200





#   / **git add .
# git commit -m "Fix: clean package-lock, add netlify.toml redirects"
# git push */